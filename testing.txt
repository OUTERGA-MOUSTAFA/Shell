Execute this as ONE atomic implementation task for MatrixBox:
improve the existing MBox HR Dashboard and employee-management experience without rewriting the existing architecture or creating duplicate subagents;
first inspect the current frontend, 
backend APIs, database models, existing dashboard components, existing simulation/video pipeline and the Media directory,
then implement a complete navigation structure for the AI Attendance System based on the existing CdC and current MBox implementation;
create a clear responsive sidebar/top navigation with Dashboard, Live Monitoring, Attendance, Employees, Import Employees, Reports/Exports, Cameras/Zones, AI Agent Activity and System Health,
keeping navigation simple enough for an HR user or technician;
create a Live Monitoring page that works with the current simulation environment and is designed to switch to production cameras later, showing the selected Media test video as the simulated camera source when running locally, the current camera/source identifier, live video/player state, detected people, recognized employees, IN/OUT events, current FPS or processing status when available, recent structured events and the processing pipeline status; the Live Monitoring page must also show the operational status of the relevant services/components such as Vision Worker, Event Bus/Redis, Backend/API, PostgreSQL, Attendance Engine and AI Agent, including last heartbeat/last activity/error state when real data is available, and must never display fake agent activity or fabricated health information; show the AI Agent activity separately from the realtime vision pipeline, making it clear that the AI Agent works on structured business data and reports and never processes raw video; MatrixBox itself must remain the development/project orchestration controller and must NOT be inserted into the production realtime pipeline; create a dedicated Attendance page showing employees with today's status including present, absent, late, early departure, break time, worked time, overtime and relevant timestamps according to the existing Attendance Engine and CdC business rules, with filters by date, employee, department and status where supported;
create a dedicated Employees page for employee management with search, filtering, employee details, matricule, name, department, active/inactive state and face-profile/enrollment information according to the existing backend design;
create a dedicated Add Employee page for manual registration of one employee, including the fields actually required by the existing database and vision/identity workflow, with validation and clear success/error feedback;
create a dedicated Import Employees page designed primarily for HR/technician bulk onboarding, supporting Excel and CSV import when practical, allowing the user to download a ready-to-use Excel/CSV template, upload a file containing one or many employees in one operation, preview and validate the rows before insertion, detect duplicate or missing matricules, detect invalid required fields, clearly show valid/invalid rows and validation errors, allow importing all valid rows or correcting the file before import, and ensure matricule is unique at the database level;
the import workflow must be safe and transactional so a malformed file cannot partially corrupt employee data;
support practical fields such as matricule, full name, department and other fields already defined by the existing project model, and do not invent unnecessary employee fields;
after employee creation/import, integrate the employee identity/profile workflow required by the Vision Worker so that employees can later be associated with face images/embeddings, but do not fake face recognition data;
create a Reports/Exports area where HR can access attendance data and export the existing supported records to Excel/CSV and prepare PDF reporting only if the current backend already supports it, otherwise leave a clearly defined extension point rather than implementing fake functionality;
create a Cameras/Zones page for technicians that displays configured cameras/sources and their health and provides access to zone configuration/calibration when the backend supports it, with the simulation source represented clearly as VideoFileSource and production sources represented as RTSP/ONVIF without changing the downstream Vision Worker pipeline;
create a System Health page that provides real health information from the backend and infrastructure where available, including API, database, Redis/Event Bus, Vision Worker, camera/source and AI Agent status, with timestamps and errors; preserve the existing architecture Camera Source → Vision Worker → Structured Events → Redis/Event Bus → Attendance Engine → PostgreSQL → Dashboard, and preserve the separation between Computer Vision and the AI Agent;
use existing APIs/contracts whenever possible and if an API is missing, coordinate backend changes through backend-eda-engineer before implementing frontend assumptions; use frontend-dashboard-engineer for the dashboard implementation, backend-eda-engineer for required API/database/import/export changes, cv-edge-engineer for camera/source/live monitoring integration if required, ai-agentic-engineer only for genuine AI Agent activity/report integration, devops-infra-engineer for service health/monitoring integration if required, security-privacy-engineer for employee/biometric access and import security review, and qa-reliability-engineer for validation of the complete dashboard and bulk-import workflows;
MatrixBox must coordinate these subagents, maintain the existing contracts, review their actual implementation and tests, resolve conflicts and integrate the final result; prioritize a clean professional HR-friendly UX, responsive layout, clear status indicators, loading/error/empty states, confirmation dialogs for destructive actions, pagination or virtualization for large employee/event lists, and accessible forms; do not create fake data just to make the UI look complete, and clearly distinguish simulation data from production data; use the existing Media test videos to validate the Live Monitoring page locally;
after implementation test the complete flow of creating one employee, importing multiple employees through the Excel/CSV workflow, validating duplicate matricules, viewing employees, viewing attendance status, running the Media simulation, receiving structured events, displaying realtime events, displaying service health and accessing AI Agent activity;
finally verify that the implementation remains compatible with the production architecture where VideoFileSource can later be replaced by RTSP/ONVIF CameraSource without changing the downstream pipeline, and give me a final report containing exactly:
implemented pages/navigation,
backend changes,
employee import format and validation rules,
simulation/live-monitoring behavior,
agent/service monitoring behavior,
tests executed and passed,
remaining limitations, and files/components modified.